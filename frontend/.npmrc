# pnpm + rolldown native bindings — rolldown does a relative require
# for its .node binary that breaks under pnpm's default symlink layout.
# Use the hoisted (npm-style) linker so the binding ends up where
# rolldown expects to find it.
node-linker=hoisted
